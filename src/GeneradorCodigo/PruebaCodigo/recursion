prueba BEGIN


TIPO_UNSIGNED FUN funcion1 ( TIPO_UNSIGNED x )
	    BEGIN

	        funcion1(x);
	        TIPO_UNSIGNED z;
            z:= 50;
            RET(x+z);
	    END;

TIPO_SINGLE x;
x:= 1.1s+25;

funcion1(TIPO_UNSIGNED(x));

END;