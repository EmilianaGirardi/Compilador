##En este codigo verifica el funcionamiento de las expresiones

prueba BEGIN
    x := 1 + 2;
    x := 1 + 2 / 2 + 7;
    x := 1 + 2 * 3;
    y := x;
    ##siguiente linea: error falta de operando en expresion
    x := 1 + ;
    ##siguiente linea: error falta de operando en expresion
    x := 1 * 2;
    x := 3 *;
END;