##En este codigo se verifica el funcionamiento de la cadena de texto

prueba BEGIN

OUTF ( [Esto es una cadena ] );
OUTF ([x+2]);
##siguiente linea: error parametro invalido
OUTF (7);
##siguiente linea: error parametro invalido
OUTF();
##siguiente linea: error parametro invalido
OUTF(aaa);
##siguiente linea: error parametro invalido
OUTF(x=3);

END ;
